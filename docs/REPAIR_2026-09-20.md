# Memhub repair batch — 2026-09-20

- [x] `chat-null-conversation-l1` — `memmy_turn` must support transports without a stable `conversation_id` without inventing a persistent conversation binding.
- [x] `control-plane-l1-unified-view` — Control Plane L1 must show the unified Memory Core + capture view and display timestamps in the browser's local timezone.
- [x] `legacy-memory-rebuild` — legacy Memmy L1 must have a controlled, evidence-preserving rebuild path into canonical L2/L3/L4 artifacts.
- [x] `architecture-reader-unified-source` — Architecture Reader must read current project architecture Markdown as well as legacy `normify-*` data.

Each item is marked complete only after its dedicated regression tests pass.

- [x] `control-plane-default-light` — Control Plane and user-facing pages must default to light theme while preserving explicit user theme choice.
- [x] `project-description-distillation` — project registry descriptions should be refreshable from evidence-backed L2 distillation without overwriting explicit manual descriptions.

Batch status: **reviewed and ready for commit**. Full verification passed with `npm test`, production dependency audit, Memory Core preflight, and `git diff --check`.


Final review additions:
- Legacy Memory Core `raw_turns` are exposed through a read-only Viewer API and merged into the L1 Control Plane view.
- Migration rebuild prefers raw-turn evidence, supplements with full legacy L1 memory detail/body, and avoids duplicate memory evidence when it references the same `RawTurn`.
- Unresolved legacy workspace scopes remain unresolved rather than being guessed into a project.
- Architecture project-doc discovery requires a repository marker, preventing nested `docs/` directories from becoming fake projects.
- Project description precedence is preserved through merge: `manual > distilled > legacy > empty`.
- L1 overview counts use aggregate queries; detailed L1 reads are paginated.

Final verification: `npm test`, `npm audit --omit=dev`, `npm run core:preflight`, `git diff --check -- .`, and changed-file secret scan all passed.
